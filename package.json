{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your project description",
  "scripts": {
    "test": "jest",                # Run Jest tests
    "test:coverage": "jest --coverage", #Run Jest tests with coverage
    "coverage": "jest --coverage --coverageReporters=text-lcov | nyc report --reporter=text-lcov"
  },
  "devDependencies": {
    "@types/jest": "^28.1.8",     # TypeScript typings for Jest
    "jest": "^28.1.3",             # Jest test framework
    "ts-jest": "^28.0.8",          # TypeScript preprocessor for Jest
    "ts-node": "^10.0.0",          #TypeScript execution environment
    #"nyc": "^15.1.0"               # Istanbul code coverage tool
    #Add any other dev dependencies your project needs
  }
}
